spring:
  application:
    name: finder
  profiles:
    active:
      default
  cloud:
    kubernetes:
      reload:
        enabled: true
        #strategy: shutdown
      config:
        sources:
          - name: config
